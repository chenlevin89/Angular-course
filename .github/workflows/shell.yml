name: Shell commands

on: [push]

jobs:
  run-shell-commands:
    runs-on: ubuntu-latest
    steps:
      - name: echo command
        run: echo "Hello world"
      - name: multi commands
        run: |
          node -v
          npm -v
